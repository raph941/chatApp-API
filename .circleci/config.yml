version: 2.1
orbs:
  heroku: circleci/heroku@1.2.2
jobs:
  deploy:
    executor: heroku/default
    steps:
      - checkout
      - heroku/install
      - run:
          command: |
            echo "Hello world"
      - heroku/deploy-via-git
      
workflows:
  my_workflow:
    jobs:
      - deploy